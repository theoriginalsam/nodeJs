{
  "name": "api-new",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon app.js",
    "gita": "git add . && git commit -m'Commit via Script' && git push origin master --force",
    "gith": "git add . && git commit -m'Commit via Script' && git push heroku master"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "express": "^4.17.3",
    "nodemon": "^2.0.4"
  }
}
